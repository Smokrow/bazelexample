py_binary(
    # The name of your Rule
    name = "main",
    # Which files should this rule use?
    srcs = ["main.py"],
    # What is the main entry point when this rule is run
    main = "main.py"
)
